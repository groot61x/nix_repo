description:::Ensure events that modify the system's Mandatory Access Controls are collected
expected1:::-w /etc/selinux -p wa -k MAC-policy -w /usr/share/selinux -p wa -k MAC-policy

manual_check:::false
